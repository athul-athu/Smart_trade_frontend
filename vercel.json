{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://smart-trade-backend-o6eq.onrender.com/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
